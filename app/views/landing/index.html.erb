<ul>
  <% @articles.each do |article| %>
    <li>
      <%= article.title %><br />
      <%= article.content %>
      <ui>
        Written by
        <%= article.author %>
        at
        <%= article.created_at %>
      </ui>
    </li>
  <% end %>
</ul>
<a href='articles/new'>new article</a>
