<ul>
  <% @articles.each do |article| %>
    <li>
      <%= article.title %><br />
      <%= article.content %>
      <p>
        Written by
        <%= article.author %>
        at
        <%= article.created_at %>
      </p>
    </li>
  <% end %>
</ul>
<a href='articles/new'>new article</a>
